package com.spring.eventHandlerBackend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class EventHandlerBackendApplication {

	public static void main(String[] args) {
		SpringApplication.run(EventHandlerBackendApplication.class, args);
	}

}

/*spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
*
* If any Problem occur inapp.properties then add that first line
*
* */

